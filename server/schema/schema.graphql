import { gql } from 'apollo-server-express';

export const typeDefenitions = gql`
  type User {
    username: String
    email: String
    password: String
  }

  type Store {
    name: String
    latitude: Number
    longitude: Number
    imageUrl: String
  }
`;